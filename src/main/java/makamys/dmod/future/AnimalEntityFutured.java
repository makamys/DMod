package makamys.dmod.future;

import net.minecraft.entity.passive.EntityAnimal;
import net.minecraft.entity.player.EntityPlayer;
import net.minecraft.item.ItemStack;

public interface AnimalEntityFutured {
/*
	public default boolean canEat(EntityAnimal dis) {
		return !dis.isInLove();
	}
	
	/** Return false to cancel the default implementation (which consumes an item if the player is not in creative mode) */
	/*public default boolean eat(EntityAnimal dis, EntityPlayer player, ItemStack stack) {
		return true;
	}*/
}
